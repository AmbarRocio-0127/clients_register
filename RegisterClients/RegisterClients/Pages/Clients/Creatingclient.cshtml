@page
@model RegisterClients.Pages.Clients.CreatingclientModel
@{
}

<br/>
<h2>Creating New Client</h2>

@if (Model.Error_.Length > 0)
{
    <div class='alert alert-warning alert-dismissible fadde show' role="alert">
        <strong>@Model.Error_ </strong>
        <button type='button' class='btn-close' data-bs-dismiss="alert" aria-label="Closr"></button>
    </div>
}

<form method="post">
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Name</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="name" value="@Model.ClientInformation.name">
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Email</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="email" value="@Model.ClientInformation.email">
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Phone</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="phone" value="@Model.ClientInformation.Phone">
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Address</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="address" value="@Model.ClientInformation.address">
        </div>

        @if (Model.Succeedprocess.Length > 0)
        {
            <div class='alert alert-warning alert-dismissible fadde show' role="alert">
                <strong>@Model.Succeedprocess</strong>
                <button type='button' class='btn-close' data-bs-dismiss="alert" aria-label="Closr"></button>
            </div>
        }

        <br /><br /><br />
        <div class="row mb-3">
            <div class="offset-sm-3 col-sm-3 d-grid">
                <button type="submit" class="btn btn-primary">Submit</button>
            </div>

            <div class="col-sm-3 d-grid">
            <a class="btn btn-outline-primary" href="/" role="button">Cancel</a>
            </div>
        </div>

    </div>
</form>